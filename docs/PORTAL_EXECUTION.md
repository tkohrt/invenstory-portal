# PORTAL_EXECUTION.md — the how

Companion to PORTAL_PLAN.md (the why). Any AI session (or human) resuming this build:
read this file top to bottom, check the Status Log, start at the first unchecked phase.
Git log is the build journal — commit after every completed phase, messages say what AND why.

## Ground rules (unskippable)
- Tenant isolation enforced by Postgres RLS; verified by the attack-probe script after EVERY migration and deploy.
- Service-role key is server-only. Secrets live in .env.local (gitignored) and Vercel env vars. Never in git, never in chat.
- Verify every CLI write with its list command. Verify deploys on the canonical alias, not hash URLs.
- Fixture UUIDs: hex characters only.
- Introspect installed SDK surfaces before writing against them (node -e "console.log(Object.keys(require('pkg')))").
- Test triggers with a real INSERT immediately after applying schema.

## Environment contract
See .env.example — those names are the contract between code, local dev, and Vercel.
Bedrock region is us-east-1 (Titan embed v2 not served on-demand in us-east-2; verified 2026-07-28).

## Accounts and identities (verified 2026-07-28)
- GitHub: tkohrt (repo owner). Commit email: 301011219+tkohrt@users.noreply.github.com
- Vercel: tyler-2114 (token verified via `vercel whoami`)
- Supabase: token verified via `projects list`. Project "Inven(s)tory" ref dafofmvbbggrmyfnjspg, region us-east-1 (co-located with Bedrock).
- AWS: account 065148797929, IAM user portal-build (Bedrock+Textract only). Root has MFA; $25 budget alarm set.
- Claude (Anthropic) model authorization: PENDING new-account restriction; not needed until Phase 6. Nova is the form-free fallback.

## Phases
Full detail per phase lives in PORTAL_PLAN.md §7. Checklist:
- [x] Phase 0 — accounts, credential verification, this repo. (2026-07-28)
- [x] Phase 1 — Next.js scaffold. (2026-07-29) tsc clean, `next build` clean, 7 routes. Preview: https://invenstory-portal.vercel.app (canonical alias verified 200 + content grep). lib/types.ts + lib/mock-data.ts + lib/data.ts are the schema contract/swap point; lib/session.tsx is the Phase 4 swap point. Dependabot alerts + security updates enabled by Tyler 2026-07-29. Vercel project: for-granted/invenstory-portal.
- [x] Phase 2 — (2026-07-29) 2 migrations applied (0001 schema incl. artifact engine + audit_log + versions; 0002 RLS on ALL 13 tables + SECURITY DEFINER helpers + private storage bucket with path-prefix policies). Seeded 2 tenants / 4 users / 2 artifact types. updated_at trigger verified with real INSERT+UPDATE across separate transactions (single-transaction test is a false negative: now() freezes per tx). Probe suite at scripts/probe.mjs — 12/12 PASS (anon reads empty on 8 tables, forged insert 42501, audit_log insert rejected, service-role sanity, private bucket listing denied). Mgmt API hardening applied+verified: disable_signup=true, password_min_length=12, SSL enforcement on. NOTE: password_hibp (leaked-password protection) is Pro-only (HTTP 402) — enable at Phase 9 upgrade. NOTE: api.supabase.com blocks default python UA (Cloudflare 1010) — always set User-Agent. DB access from sandbox via session pooler aws-0-us-east-1.pooler.supabase.com:5432, user postgres.dafofmvbbggrmyfnjspg. SIGN-OFF: Tyler reviewed probe output and approved, 2026-07-29. Phase 2 CLOSED.
- [~] Phase 3 — SPLIT: 3a DONE (2026-07-29): pages converted to server-fetch + client-view; lib/server/{db,session,actions,data}.ts; mock module DELETED (compiler-verified); mock identity now an httpOnly cookie session shaped like Phase 4's real one; local smoke tests grep real DB content incl. admin/client separation and no-cookie 307s; Vercel env vars set+VERIFIED for production AND preview (preview adds prompt for git branch non-interactively — CLI hangs; use REST API v10 /env?upsert=true instead). TEMPORARY: service-role client with explicit tenant filter until Phase 4 RLS sessions. 3b DONE (2026-07-29): /api/upload (session-gated, 25MB cap, tenant-path storage, doc+version+tags rows, inline processing) + /api/ingest reprocess. lib/server/ingest.ts: pdf-parse v2 CLASS API (PDFParse — v1 function API from training data is gone; introspect!), mammoth for docx, txt/md/html direct; chunking with page numbers + char offsets; Titan embeddings per chunk. next.config serverExternalPackages=[pdf-parse,mammoth] (bundling breaks pdf.js worker). RESILIENCE: embed failure degrades to text-searchable doc with error_detail='semantic index pending', backfill via /api/ingest. VERIFIED: real 2-page PDF → 2 chunks w/ correct pages/offsets, status ready. OPEN AWS ITEM: account-level Bedrock invocation now blocked ('Operation not allowed', ValidationException) in ALL regions — new-account restriction propagated AFTER an earlier successful call; covers Titan too, not just Anthropic. ACTION (Tyler): retry ~24h; if persists, AWS support case (Account and billing). Embeddings for 3 test chunks backfill via /api/ingest after unblock. DEFERRED in 3b: Textract OCR for scanned PDFs (needs S3 staging bucket + IAM update), audio transcription (Transcribe), xlsx extraction — all fail loudly with clear error_detail, never silently.
- [x] Phase 4 — CORE DONE (2026-07-29): real Supabase Auth. Mock cookie REPLACED. @supabase/ssr userClient() carries user session -> RLS enforces tenancy (service-role scaffold RETIRED from data.ts; db.ts now only ingestion + identity resolution). 4 auth users created+linked to app_user.auth_id, passwords in /tmp/user-passwords.txt (-> password manager, NOT git). proxy.ts route protection (Next16 middleware). /auth/callback handles ?code= AND ?token_hash= (playbook #11). Password reset (email-agnostic notice), update-password page. Redirect allowlist + site_url set via mgmt API. NEW PROBE SUITE scripts/probe-auth.mjs: signs in as REAL client Lili, 8/8 PASS incl. cannot-read/forge/leak KHAI docs+cards+chunks+audit_log (the logged-in-attacker test). Anon suite still 12/12. RESEND: account created, send-only API key stored (Vercel all targets + .env.local). Supabase custom SMTP CONFIGURED via mgmt API (smtp.resend.com:465, user=resend, sender noreply@forgranted.com). PENDING: forgranted.com domain verification — send-only key can't manage domains via API, so Tyler adds the domain in the Resend DASHBOARD (generates DNS records) and adds them to GoDaddy. Auth email delivery activates on verification; until then admin-created passwords work. TOTP MFA: enable at checkpoint (needs a real authenticator enrollment). SIGN-OFF: Tyler 2026-07-29 — verified branded email delivery (real reset email received), admin login = full cross-tenant access, client login (Lili) sees only Fund The Climb and is redirected off /admin/reviews to /library. Auth probe 8/8 + anon 12/12. Phase 4 CLOSED. (MFA enrollment optional, deferrable; Resend/SMTP fully live.)
- [x] Phase 5 — (2026-07-29) Postgres FTS. Migration 0003: tsvector generated columns on document(title+snippet) AND document_chunk(text) + GIN indexes; search_inventory(p_query) RPC (SECURITY INVOKER -> runs under caller RLS; best-passage-per-doc via distinct-on, ts_headline with NON-HTML <<hl>> sentinels). /api/search route (userClient RPC, admin tenant-scope, layer+tag filters, audit_log write). SearchView: live debounced search, XSS-safe passage render (escape THEN swap sentinels for <mark>). Passage-level hits over real chunk text WITH page numbers verified (e.g. 'diversify settlement' -> PDF page 2). Probe suite extended to 10/10: search RPC returns 0 foreign-tenant hits, finds own docs. Anon 12/12. NOTE: seeded docs match via title/snippet; ingested docs match full chunk text — both paths covered so search works pre- and post-ingestion.
- [~] Phase 6 — SCAFFOLDED against Bedrock block (2026-07-29). Migrations: 0004 match_chunks (pgvector cosine, hnsw index, SECURITY INVOKER->RLS scopes tenant BEFORE similarity), 0005 chat_message.author_user_id, 0006 match_lexical (OR-recall retrieval for RAG, distinct from AND-precise search_inventory). lib/server/rag.ts: retrieve() = semantic(embed+match_chunks)+lexical hybrid, auto-falls-back to lexical-only when Bedrock blocked; generate() = Bedrock ConverseCommand (model-agnostic API, introspected) with HONEST extractive fallback (top passages+citations) when CHAT_MODEL unset/blocked. /api/chat: auth-gated, DB-backed per-user rate limit (12/min), persists chat_session/chat_message, audit_log write. ChatView: real endpoint, live citations, extractive-mode notice, busy states. scripts/eval-models.mjs ready to pick model on unblock. VERIFIED LIVE via real Lili session: 'founding story' cites ONLY her FTC interview, never KHAI's Howie interview (isolation through full RAG path). Probes now 12/12 incl. match_lexical + match_chunks cross-tenant = 0. LIGHTS UP ON UNBLOCK: (1) backfill embeddings via /api/ingest, (2) run eval-models, (3) set BEDROCK_CHAT_MODEL_ID env -> generative answers, zero code change. Awaiting Bedrock activation + Tyler checkpoint.
- [x] Phase 7 — (2026-07-29) Story Intelligence artifact engine. lib/server/si-registry.ts = per-type registry (system prompt + buildPrompt + zod cardSchema + grounded fallback + gapNote); adding a type = 1 registry entry + card renderer + DB row. lib/server/artifacts.ts = generic generate(): loadCorpus -> synthesize (Bedrock ConverseCommand ARMED; grounded fallback while blocked, model_used='scaffold') -> zod validate (every card MUST cite) -> resolve citation titles->doc ids -> upsert pending set + cards -> Slack webhook post. artifact-actions.ts = generate/approve/regenerate/removeCard/editCard (admin-gated, audited). Stale-on-upload wired into /api/upload. ArtifactPanel drives full lifecycle live. /api/si/generate harness. VERIFIED: generated FTC themes (4 grounded cards w/ real citations), stale-on-upload flip (approved->stale after Lili upload), and GENERICNESS PROOF — added 3rd type 'fundability_snapshot' with ONLY registry+DB rows, generated through identical engine/route/actions, zero engine changes. Demo state: FTC impact_metrics=approved(2), KHAI themes=pending(3), rest none. LIGHTS UP ON BEDROCK: set BEDROCK_CHAT_MODEL_ID -> synth becomes real LLM map-reduce, zero code change. Slack: needs tenant.slack_webhook_url set per client to post (skips quietly otherwise).
- [x] Phase 8 — (2026-07-29) Grant Drafts 'In the Works'. Migration 0007: grant_draft + draft_bracket tables, RLS (admin-write, client-answer, tenant-select). lib/server/drafts.ts (parseBrackets, getDrafts/getDraft), draft-actions.ts (saveDraft w/ bracket sync, setDraftStatus pipeline drafting->client_review->submitted->won/lost, answerBracket). KEY MECHANISM: answerBracketAction files each answer back into the Inven(s)tory as a real Layer II 'note' doc (storage + document + version + tags grant-answer/captured) run through processDocument (chunk + embed-or-text), links filed_document_id, marks SI stale. DraftsView (status pills, answer progress bars, admin New Draft), DraftDetailView (body with inline [BRACKET] chips that turn green when filled, question cards, admin status controls). Nav 'In the Works'. Seeded FTC demo draft (ODH SUD, 5 brackets, client_review). Probes 17/17 incl. grant_draft/draft_bracket cross-tenant + admin-only-write. Money as integer cents.
- [ ] Phase 8.5 — HARDENING GATE (unskippable): fresh-eyes red team, hostile-document injection probe, upload hygiene, rate limits, headers, session behavior, audit-log spot check; real-device pass by BOTH founders; Lighthouse a11y 90+; sign-off recorded here.
- [ ] Phase 9 — Supabase Pro + Vercel Pro (HUMAN buys), portal.forgranted.com DNS (HUMAN adds CNAME), full probe suite vs production, backup restore demonstrated, then first client invite.

## Dependency security posture (2026-07-29)
- postcss + sharp advisories (production-relevant, shipped in Next): FIXED via package.json overrides (postcss ^8.5.18 -> 8.5.24, sharp ^0.35.0 -> 0.35.3). Keep overrides until Next bumps its pins, then remove.
- brace-expansion DoS advisories: residual HIGH flags confined to the ESLint dev toolchain (linter only, never deployed; exploitation requires hostile glob input to the linter on a dev machine). No non-breaking fix exists today: the fix chain requires eslint@10, which breaks eslint-plugin-react (verified: getFilename crash). DECISION: accept + document, dismiss Dependabot alerts with reason "vulnerable code not invoked at runtime", RE-CHECK at Phase 8.5 gate — ecosystem patch will likely land well before then.
- eslint@9 retained; lint is clean and caught one real bug (setState-in-effect in Shell.tsx, fixed by closing nav on link tap).

## Status log
- 2026-07-29: BUG+FIX — prod 500 on every route: NEXT_PUBLIC_SUPABASE_URL was never set on Vercel (only non-public SUPABASE_URL), so proxy.ts middleware got undefined URL and threw. NEXT_PUBLIC_ vars must be set BEFORE the build that inlines them. Added to all targets; redeployed. Lesson: middleware failures 500 the WHOLE site — test canonical alias after every deploy (playbook #6 cousin).
- 2026-07-29: DECISION (Tyler) — every SI artifact reads all three layers by default; corpus_filter reserved for types that quote a layer verbatim. impact_metrics filter removed. Admin-only Regenerate added to approved sets.
- 2026-07-29: NAMING — client-facing term for artifact-engine output is "Story Intelligence"; SI panels moved off Library onto /story-intelligence/[slug] pages (registry-driven nav). Internal name remains artifact engine.
- 2026-07-28: Phase 0 executed. All four credentials verified by live calls (sts get-caller-identity; Titan embed test call returned 1024-dim vector in us-east-1; supabase projects list; vercel whoami; GitHub API /user).
- 2026-07-28: FINDING — Supabase project "Inven(s)tory" was created in Canada (Central). Recommended: recreate in us-east-1 (co-locate with Bedrock; keep client data + AI processing in one US region) while project is empty. Region cannot be changed after creation. RESOLVED 2026-07-29: Canada project deleted via Management API; recreated in us-east-1 (ref dafofmvbbggrmyfnjspg). DB password in password manager + .env.local (SUPABASE_DB_PASSWORD).
- 2026-07-28: FINDING — GitHub PAT is fine-grained and could not create repos (Administration permission missing). Repo created manually; token needs Contents read/write for push.
- 2026-07-28: Claude use-case form blocked by new-account restriction; retry after 24h or AWS support case (Account and billing, free tier).

## Paste this to the next AI
"Continue the Inven(s)tory Portal build. Repo: github.com/tkohrt/invenstory-portal. Read docs/PORTAL_EXECUTION.md, check the Status Log, start at the first unchecked phase. Credentials are in .env.local locally / ask Tyler to paste tokens. Never skip a probe checkpoint."
